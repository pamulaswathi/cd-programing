%{
#include<stdio.h>
%}

letter[a-zA-Z]
digit[0-9]

%%
{letter}({letter}|{digit})*  {printf("%s is a identifier", yytext);}
{digit}({digit}|{digit})*  {printf("%s is not a identifier", yytext);}
%%

int yywrap()
{
return 0;
}
void main()
{
yylex();
}
